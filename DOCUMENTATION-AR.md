# التوثيق الشامل - ثيم SWA Visa Commerce

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [بنية القالب](#بنية-القالب)
3. [الميزات التقنية الشاملة](#الميزات-التقنية-الشاملة)
4. [نظام إدارة الطلبات](#نظام-إدارة-الطلبات)
5. [صفحة طلب الرحلة المخصصة](#صفحة-طلب-الرحلة-المخصصة)
6. [نموذج التواصل](#نموذج-التواصل)
7. [صفحة حساب المستخدم](#صفحة-حساب-المستخدم)
8. [التكامل مع WooCommerce](#التكامل-مع-woocommerce)
9. [الأمان في رفع الملفات](#الأمان-في-رفع-الملفات)
10. [التكامل مع أنظمة CRM الخارجية](#التكامل-مع-أنظمة-crm-الخارجية)
11. [جميع الميزات بالتفصيل](#جميع-الميزات-بالتفصيل)
12. [التحسينات من النظام القديم](#التحسينات-من-النظام-القديم)

---

## نظرة عامة

**ثيم SWA Visa Commerce** هو ثيم WordPress متخصص ومتطور بالكامل لإدارة خدمات التأشيرات. تم تطويره كحل مخصص بالكامل بدلاً من استخدام الإضافات الجاهزة مثل Trip Engine Plugin. يوفر الثيم نظاماً متكاملاً وشاملاً لإدارة جميع جوانب أعمال التأشيرات من عرض الخدمات إلى معالجة الطلبات وإدارة المستندات.

### المميزات الرئيسية:
- ✅ حل مخصص بالكامل مصمم خصيصاً لخدمات التأشيرات
- ✅ نظام إدارة طلبات متقدم مع تتبع كامل للحالات
- ✅ تكامل كامل مع WooCommerce للمدفوعات
- ✅ نظام أمان متقدم لرفع وإدارة المستندات
- ✅ لوحة تحكم إدارية شاملة
- ✅ صفحة حساب مستخدم متكاملة
- ✅ دعم ثنائي اللغة (عربي/إنجليزي) مع RTL/LTR
- ✅ تصميم متجاوب بالكامل
- ✅ قابل للتكامل مع أنظمة CRM الخارجية

---

## بنية القالب

### الهيكل التنظيمي للملفات

```
swa/
├── assets/                          # الموارد الثابتة
│   ├── css/                         # ملفات التنسيق
│   │   ├── theme.css               # التنسيقات الأساسية
│   │   ├── rtl.css                 # تنسيقات RTL للعربية
│   │   ├── account.css              # تنسيقات صفحة الحساب
│   │   ├── admin.css               # تنسيقات لوحة التحكم
│   │   ├── contact.css              # تنسيقات صفحة التواصل
│   │   ├── trip-request.css        # تنسيقات صفحة طلب الرحلة
│   │   ├── woocommerce.css         # تنسيقات WooCommerce
│   │   ├── visa-carousel.css       # تنسيقات كاروسيل التأشيرات
│   │   ├── filter.css              # تنسيقات الفلاتر
│   │   ├── family-applications.css # تنسيقات طلبات العائلات
│   │   └── sla.css                 # تنسيقات لوحة SLA
│   ├── js/                          # ملفات JavaScript
│   │   ├── main.js                 # الملف الرئيسي
│   │   ├── account.js              # وظائف صفحة الحساب
│   │   ├── admin.js                # وظائف لوحة التحكم
│   │   ├── contact.js               # وظائف نموذج التواصل
│   │   ├── trip-request.js         # وظائف طلب الرحلة
│   │   ├── filter.js               # وظائف الفلاتر
│   │   ├── visa-carousel.js        # وظائف كاروسيل التأشيرات
│   │   └── family-applications.js  # وظائف طلبات العائلات
│   └── images/                      # الصور والموارد
│
├── inc/                             # ملفات PHP الرئيسية
│   ├── class-swa-theme.php         # الفئة الرئيسية للقالب
│   ├── class-swa-cpt.php           # أنواع المحتوى المخصصة
│   ├── class-swa-woocommerce.php   # التكامل مع WooCommerce
│   ├── class-swa-applications.php  # إدارة طلبات التأشيرات
│   ├── class-swa-documents.php     # إدارة المستندات
│   ├── class-swa-account.php       # صفحة حساب المستخدم
│   ├── class-swa-admin.php         # لوحة التحكم الإدارية
│   ├── class-swa-contact.php       # نموذج التواصل
│   ├── class-swa-trip-requests.php # إدارة طلبات الرحلات
│   ├── class-swa-elementor.php     # التكامل مع Elementor
│   ├── class-swa-ajax-filter.php  # الفلاتر والبحث
│   ├── class-swa-family-applications.php # طلبات العائلات
│   ├── class-swa-whatsapp.php      # التكامل مع WhatsApp
│   ├── class-swa-sla.php           # نظام SLA
│   ├── class-swa-retention.php    # نظام الاحتفاظ بالبيانات
│   ├── class-swa-acf.php           # التكامل مع ACF
│   ├── class-swa-destination-metabox.php # معلومات الوجهات
│   ├── class-swa-home-export.php   # تصدير بيانات الصفحة الرئيسية
│   ├── template-functions.php      # دوال القوالب
│   ├── template-tags.php           # علامات القوالب
│   └── elementor/                  # ويدجتات Elementor
│       └── widgets/
│           ├── class-swa-visa-card-widget.php
│           ├── class-swa-visa-grid-widget.php
│           ├── class-swa-visa-filter-widget.php
│           ├── class-swa-visa-carousel-widget.php
│           ├── class-swa-country-list-widget.php
│           ├── class-swa-requirements-widget.php
│           └── class-swa-destination-carousel-widget.php
│
├── templates/                       # قوالب الصفحات
│   ├── admin/                      # قوالب لوحة التحكم
│   │   ├── dashboard.php
│   │   ├── new-applications.php
│   │   └── reports.php
│   ├── myaccount/                  # قوالب حساب المستخدم
│   │   ├── visa-applications.php
│   │   ├── visa-application-detail.php
│   │   ├── trip-requests.php
│   │   └── trip-request-detail.php
│   └── demo-visas-page.php
│
├── woocommerce/                    # قوالب WooCommerce المخصصة
│   └── myaccount/
│       └── form-login.php
│
├── template-parts/                 # أجزاء القوالب
│   ├── content.php
│   ├── content-post.php
│   └── content-visa-card.php
│
├── languages/                      # ملفات الترجمة
│   └── swa.pot
│
├── functions.php                   # ملف الوظائف الرئيسي
├── header.php                      # رأس الصفحة
├── footer.php                      # تذييل الصفحة
├── index.php                       # القالب الافتراضي
├── page.php                        # قالب الصفحات
├── page-home.php                   # قالب الصفحة الرئيسية
├── page-contact.php                # قالب صفحة التواصل
├── page-trip-request.php           # قالب صفحة طلب الرحلة
├── single-visa_service.php         # قالب صفحة خدمة التأشيرة
├── single-destination.php         # قالب صفحة الوجهة
├── archive-visa_service.php       # قالب أرشيف التأشيرات
├── archive-destination.php        # قالب أرشيف الوجهات
├── taxonomy-visa_country.php     # قالب تصنيف الدول
├── taxonomy-visa_type.php        # قالب تصنيف أنواع التأشيرات
├── woocommerce.php                # قالب WooCommerce
└── style.css                      # ملف التنسيق الرئيسي
```

### المكونات الرئيسية

#### 1. الفئات الأساسية (Core Classes)
- **SWA_Theme**: الفئة الرئيسية التي تدير جميع وظائف القالب
- **SWA_CPT**: تسجيل أنواع المحتوى المخصصة والتصنيفات
- **SWA_ACF**: التكامل مع Advanced Custom Fields
- **SWA_Elementor**: التكامل مع Elementor Page Builder

#### 2. فئات الوظائف (Feature Classes)
- **SWA_WooCommerce**: التكامل الكامل مع WooCommerce
- **SWA_Applications**: إدارة طلبات التأشيرات
- **SWA_Documents**: إدارة المستندات المرفوعة
- **SWA_Account**: صفحة حساب المستخدم
- **SWA_Admin**: لوحة التحكم الإدارية
- **SWA_Contact**: نموذج التواصل
- **SWA_Trip_Requests**: إدارة طلبات الرحلات المخصصة

#### 3. فئات الميزات المتقدمة (Advanced Features)
- **SWA_Family_Applications**: طلبات العائلات والمسافرين المتعددين
- **SWA_WhatsApp**: التكامل مع WhatsApp
- **SWA_SLA**: نظام Service Level Agreement
- **SWA_Retention**: نظام الاحتفاظ بالبيانات
- **SWA_Ajax_Filter**: الفلاتر والبحث المتقدم

---

## الميزات التقنية الشاملة

### 1. دعم ثنائي اللغة (Bilingual Support)

#### التطبيق:
- **اللغات المدعومة**: العربية (افتراضي) والإنجليزية
- **اتجاه النص**: RTL للعربية و LTR للإنجليزية
- **الخطوط**:
  - العربية: IBM Plex Sans Arabic
  - الإنجليزية: DM Sans
  - العناوين: Playfair Display

#### الميزات:
- تبديل تلقائي للاتجاه حسب اللغة
- حقول مخصصة ثنائية اللغة لجميع المحتويات
- دعم WPML و Polylang
- ترجمة تلقائية لواجهة WooCommerce
- ترجمة جميع النصوص والرسائل

#### الكود:
```php
// دالة التحقق من اللغة الحالية
function swa_is_arabic() {
    return swa_get_current_language() === 'ar';
}

// دالة الحصول على الحقل ثنائي اللغة
function swa_get_bilingual_field( $field_name, $post_id = null ) {
    $lang = swa_get_current_language();
    $field_key = $field_name . '_' . $lang;
    $value = get_field( $field_key, $post_id );
    
    // Fallback للعربية إذا كانت الإنجليزية فارغة
    if ( empty( $value ) && $lang === 'en' ) {
        $value = get_field( $field_name . '_ar', $post_id );
    }
    
    return $value;
}
```

### 2. أنواع المحتوى المخصصة (Custom Post Types)

#### أ. خدمات التأشيرات (visa_service)
- **الوصف**: نوع محتوى مخصص لإدارة جميع خدمات التأشيرات
- **الميزات**:
  - دعم العنوان والمحتوى والوصف
  - صورة مميزة لكل خدمة
  - حقول مخصصة عبر ACF:
    - السعر (`_visa_price`)
    - سعر التخفيض (`_visa_sale_price`)
    - العنوان بالعربية (`_visa_title_ar`)
    - العنوان بالإنجليزية (`_visa_title_en`)
    - مدة المعالجة (`_visa_processing_time`)
    - صلاحية التأشيرة (`_visa_validity`)
    - مدة الإقامة (`_visa_stay_duration`)
    - نوع الدخول (`_visa_entry_type`)
    - المستندات المطلوبة (`_visa_documents`)
    - منتج WooCommerce المرتبط (`_visa_woo_product`)
  - التصنيفات:
    - تصنيف الدول (`visa_country`)
    - نوع التأشيرة (`visa_type`)
    - الأهلية (`visa_eligibility`)
  - رابط مخصص: `/visa/{slug}/`

#### ب. طلبات التأشيرات (visa_application)
- **الوصف**: نوع محتوى خاص لتتبع طلبات التأشيرات
- **الميزات**:
  - إنشاء تلقائي عند إتمام الدفع
  - غير قابل للوصول العام (private)
  - حقول مخصصة:
    - رقم الطلب (`_app_order_id`)
    - معرف المستخدم (`_app_user_id`)
    - خدمة التأشيرة (`_app_visa_service`)
    - الحالة (`_app_status`)
    - معلومات المتقدمين (`_app_applicants`)
    - تاريخ السفر (`_app_travel_date`)
    - غرض السفر (`_app_travel_purpose`)
    - المستندات (`_app_documents`)
    - التأشيرة النهائية (`_app_final_visa`)
    - سجل الحالات (`_app_status_history`)
    - الملاحظات الداخلية (`_app_internal_notes`)
    - ملاحظات العميل (`_app_customer_notes`)

#### ج. الوجهات (destination)
- **الوصف**: صفحات مخصصة للدول والوجهات السياحية
- **الميزات**:
  - معلومات تفصيلية عن كل وجهة
  - صورة مميزة
  - ربط مع تصنيف الدول
  - رابط مخصص: `/destination/{slug}/`

#### د. طلبات الرحلات (trip_request)
- **الوصف**: نظام مخصص لطلبات تصميم الرحلات
- **الميزات**:
  - معلومات العميل الكاملة
  - تفاصيل الرحلة
  - الوجهة (موجودة أو مخصصة)
  - تواريخ السفر
  - عدد الأفراد (كبار، أطفال، رضع)
  - أعمار الأطفال
  - الميزانية
  - مستوى الفنادق
  - الاهتمامات والأنشطة
  - ملاحظات إضافية
  - حالات الطلب:
    - قيد المراجعة
    - قيد الدراسة
    - تم تقديم عرض
    - موافق عليه
    - مرفوض
    - مكتمل
    - ملغي

### 3. التصنيفات المخصصة (Custom Taxonomies)

#### أ. تصنيف الدول (visa_country)
- **النوع**: تصنيف هرمي
- **الميزات**:
  - دعم التصنيفات الفرعية
  - رابط مخصص: `/country/{slug}/`
  - حقول إضافية:
    - رمز الدولة (`_country_code`)
    - العلم (من خلال الرمز)

#### ب. أنواع التأشيرات (visa_type)
- **النوع**: تصنيف غير هرمي
- **الأنواع الافتراضية**:
  - سياحية (tourist)
  - تجارية (business)
  - ترانزيت (transit)
  - إلكترونية (evisa)
  - عمل (work)
  - دراسية (student)
  - علاجية (medical)
  - زيارة عائلية (family)

#### ج. الأهلية (visa_eligibility)
- **النوع**: تصنيف غير هرمي
- **التصنيفات**:
  - مواطنون سعوديون
  - مقيمون
  - مواطنو دول الخليج
  - مقيمو دول الخليج
  - جميع الجنسيات

### 4. نظام SEO المتقدم

#### الميزات:
- **Schema Markup**: دعم كامل لـ Schema.org
  - Organization Schema
  - Product Schema (لخدمات التأشيرات)
  - Article Schema (للمدونة)
  - Breadcrumb Schema
  - Place Schema (للوجهات)
- **Meta Tags**:
  - Open Graph tags
  - Twitter Card tags
  - Canonical URLs
  - Robots meta
  - Language tags (hreflang)
- **التوافق**: متوافق مع Yoast SEO

---

## نظام إدارة الطلبات

### 1. إنشاء الطلبات التلقائي

#### آلية العمل:
عند إتمام دفع طلب WooCommerce، يتم إنشاء طلب تأشيرة تلقائياً:

```php
// في class-swa-woocommerce.php
public function create_application_on_order_complete( $order_id ) {
    $order = wc_get_order( $order_id );
    
    foreach ( $order->get_items() as $item ) {
        $product_id = $item->get_product_id();
        $visa_service_id = get_post_meta( $product_id, '_visa_service_id', true );
        
        // إنشاء طلب التأشيرة
        $app_id = wp_insert_post( array(
            'post_type'   => 'visa_application',
            'post_title'  => sprintf( 'طلب تأشيرة #%d - %s', $order_id, get_the_title( $visa_service_id ) ),
            'post_status' => 'publish',
        ) );
        
        // حفظ البيانات
        update_field( '_app_order_id', $order_id, $app_id );
        update_field( '_app_user_id', $order->get_user_id(), $app_id );
        update_field( '_app_visa_service', $visa_service_id, $app_id );
        update_field( '_app_status', 'payment_confirmed', $app_id );
        
        // إنشاء سجل الحالات
        $history = array(
            array(
                'status' => 'submitted',
                'date'   => $order->get_date_created()->format( 'Y-m-d H:i:s' ),
                'user'   => $order->get_user_id(),
                'note'   => 'تم إنشاء الطلب',
            ),
            array(
                'status' => 'payment_confirmed',
                'date'   => current_time( 'Y-m-d H:i:s' ),
                'user'   => $order->get_user_id(),
                'note'   => 'تم تأكيد الدفع',
            ),
        );
        update_field( '_app_status_history', $history, $app_id );
    }
}
```

### 2. حالات الطلبات (Order Statuses)

#### الحالات المتاحة:
1. **تم الإرسال** (submitted)
   - الحالة الأولية عند إنشاء الطلب
   
2. **تم تأكيد الدفع** (payment_confirmed)
   - بعد إتمام الدفع بنجاح
   - يتطلب إجراء من العميل (رفع المستندات)
   
3. **مراجعة المستندات** (documents_review)
   - عند بدء مراجعة المستندات المرفوعة
   - حالة داخلية (لا يتم إشعار العميل)
   
4. **مستندات ناقصة** (missing_documents)
   - عند الحاجة لمستندات إضافية
   - يتطلب إجراء من العميل
   - يتم إرسال قائمة بالمستندات المطلوبة
   
5. **تم الإرسال للسفارة** (submitted_embassy)
   - بعد إرسال الطلب للسفارة
   
6. **قيد المعالجة** (under_processing)
   - أثناء معالجة الطلب في السفارة
   
7. **تمت الموافقة** (approved)
   - بعد موافقة السفارة على الطلب
   
8. **مرفوض** (rejected)
   - عند رفض الطلب من السفارة
   
9. **تم إصدار التأشيرة** (visa_issued)
   - بعد إصدار التأشيرة
   - يمكن رفع ملف التأشيرة النهائي
   
10. **مكتمل** (completed)
    - بعد تسليم التأشيرة للعميل
    - نهاية العملية
    
11. **ملغي** (cancelled)
    - عند إلغاء الطلب

#### سجل الحالات (Status History):
- يتم حفظ كل تغيير في الحالة مع:
  - التاريخ والوقت
  - المستخدم الذي قام بالتغيير
  - الملاحظة (إن وجدت)
- يمكن عرض السجل الكامل في لوحة التحكم

### 3. إدارة المتقدمين (Applicants Management)

#### معلومات المتقدم:
- الاسم بالعربية (`name_ar`)
- الاسم بالإنجليزية (`name_en`)
- رقم جواز السفر (`passport_number`)
- الجنسية (`nationality`)
- تاريخ الميلاد (`date_of_birth`)
- الجنس (`gender`)
- رقم الجوال (`phone`)
- البريد الإلكتروني (`email`)
- صلة القرابة (`relation`) - للمسافرين الإضافيين

#### دعم المتقدمين المتعددين:
- يمكن إضافة عدة متقدمين في طلب واحد
- يتم تحديد عدد المتقدمين في صفحة الدفع
- يتم إنشاء placeholders للمتقدمين عند إنشاء الطلب
- يمكن للعميل ملء معلومات كل متقدم لاحقاً

### 4. تتبع الطلبات (Order Tracking)

#### للعملاء:
- عرض جميع الطلبات في صفحة الحساب
- عرض تفاصيل كل طلب:
  - معلومات خدمة التأشيرة
  - حالة الطلب الحالية
  - سجل التغييرات
  - المستندات المرفوعة
  - المستندات المطلوبة
  - معلومات المتقدمين
  - التأشيرة النهائية (عند الإصدار)

#### للإدارة:
- لوحة تحكم شاملة لعرض جميع الطلبات
- فلترة حسب الحالة
- بحث متقدم
- إجراءات مجمعة
- إحصائيات مفصلة

---

## صفحة طلب الرحلة المخصصة

### نظرة عامة

تم تطوير صفحة طلب الرحلة كحل مخصص بالكامل بدلاً من استخدام إضافة Trip Engine Plugin. الصفحة توفر نظاماً متكاملاً لتصميم الرحلات المخصصة.

### الملفات المرتبطة

- **Template**: `page-trip-request.php`
- **Class**: `inc/class-swa-trip-requests.php`
- **JavaScript**: `assets/js/trip-request.js`
- **CSS**: `assets/css/trip-request.css`
- **Template Detail**: `templates/myaccount/trip-request-detail.php`

### الميزات التفصيلية

#### 1. نموذج طلب الرحلة

##### أ. معلومات شخصية:
- **الاسم الكامل** (مطلوب)
- **البريد الإلكتروني** (اختياري)
- **رقم الجوال** (مطلوب)

##### ب. تفاصيل الرحلة:
- **تفاصيل الرحلة**: وصف نصي للرحلة المطلوبة
- **الوجهة المرغوبة**:
  - خيار 1: من الوجهات الموجودة
    - قائمة منسدلة بجميع الوجهات المسجلة
  - خيار 2: وجهة مخصصة
    - حقل نصي لكتابة الوجهة

##### ج. تواريخ السفر:
- **تاريخ البدء** (مطلوب)
- **تاريخ الانتهاء** (مطلوب)
- التحقق من أن التاريخ بعد 7 أيام على الأقل

##### د. عدد الأفراد:
- **الكبار**: عدد الكبار (افتراضي: 1)
- **الأطفال**: عدد الأطفال (افتراضي: 0)
- **الرضع**: عدد الرضع (افتراضي: 0)
- **أعمار الأطفال**: 
  - يظهر تلقائياً عند إضافة أطفال
  - حقل لكل طفل لعمره

##### هـ. التفضيلات:
- **الميزانية التقريبية للشخص الواحد** (ريال سعودي)
- **مستوى الفنادق**:
  - 3 نجوم
  - 4 نجوم
  - 5 نجوم
- **الاهتمامات والأنشطة** (متعدد):
  - الشواطئ
  - المغامرات
  - الثقافة والتاريخ
  - التسوق
  - الطبيعة
  - الحياة الليلية
  - عائلي
  - رومانسي
  - الطعام والمطاعم
  - الرياضة

##### و. ملاحظات إضافية:
- حقل نصي كبير لملاحظات أو متطلبات خاصة

#### 2. معالجة الطلب

##### آلية الحفظ:
```php
// في class-swa-trip-requests.php
public function ajax_submit_request() {
    // التحقق من البيانات
    // إنشاء طلب رحلة
    $request_id = wp_insert_post( array(
        'post_type'   => 'trip_request',
        'post_title'  => sprintf( 'طلب رحلة - %s', $full_name ),
        'post_status' => 'publish',
    ) );
    
    // حفظ جميع البيانات في post meta
    update_post_meta( $request_id, '_trip_full_name', $full_name );
    update_post_meta( $request_id, '_trip_email', $email );
    update_post_meta( $request_id, '_trip_phone', $phone );
    // ... باقي الحقول
    
    // إرسال إشعار للإدارة
    $this->send_admin_notification( $request_id );
}
```

#### 3. حالات طلبات الرحلات

- **قيد المراجعة** (pending): الحالة الافتراضية
- **قيد الدراسة** (reviewing): عند بدء دراسة الطلب
- **تم تقديم عرض** (quoted): بعد إرسال عرض السعر
- **موافق عليه** (approved): عند موافقة العميل
- **مرفوض** (rejected): عند رفض الطلب
- **مكتمل** (completed): بعد إتمام الرحلة
- **ملغي** (cancelled): عند إلغاء الطلب

#### 4. عرض الطلبات

##### للعملاء:
- عرض جميع طلبات الرحلات في صفحة الحساب
- عرض تفاصيل كل طلب:
  - معلومات العميل
  - الوجهة
  - تواريخ السفر
  - عدد الأفراد وأعمار الأطفال
  - التفضيلات
  - الحالة الحالية
  - تاريخ الطلب

##### للإدارة:
- عرض جميع الطلبات في لوحة التحكم
- أعمدة مخصصة:
  - العميل
  - الوجهة
  - تواريخ السفر
  - عدد الأفراد
  - الحالة
- إمكانية تحديث الحالة
- إرسال عروض أسعار
- إدارة كاملة للطلبات

### الفرق عن Trip Engine Plugin

#### التحسينات:
1. **حل مخصص**: تم تطويره خصيصاً لاحتياجات التأشيرات
2. **مرونة أكبر**: إمكانية تخصيص كاملة
3. **تكامل أفضل**: تكامل كامل مع نظام التأشيرات
4. **واجهة محسنة**: تصميم أفضل وأسهل في الاستخدام
5. **أداء أفضل**: كود محسّن بدون إضافات غير ضرورية

---

## نموذج التواصل

### نظرة عامة

نموذج تواصل متكامل وآمن مع إدارة كاملة للرسائل الواردة.

### الملفات المرتبطة

- **Template**: `page-contact.php`
- **Class**: `inc/class-swa-contact.php`
- **JavaScript**: `assets/js/contact.js`
- **CSS**: `assets/css/contact.css`
- **Post Type**: `contact_submission`

### الميزات التفصيلية

#### 1. حقول النموذج

- **الاسم الكامل** (مطلوب)
- **البريد الإلكتروني** (مطلوب)
- **رقم الهاتف** (مطلوب)
- **الموضوع** (مطلوب)
- **الرسالة** (مطلوب)

#### 2. التحقق من البيانات (Validation)

```php
// في class-swa-contact.php
public function ajax_submit_contact_form() {
    // التحقق من البيانات
    if ( empty( $name ) || empty( $email ) || empty( $phone ) || empty( $subject ) || empty( $message ) ) {
        wp_send_json_error( array( 'message' => 'يرجى ملء جميع الحقول المطلوبة.' ) );
    }
    
    // التحقق من صحة البريد الإلكتروني
    if ( ! is_email( $email ) ) {
        wp_send_json_error( array( 'message' => 'البريد الإلكتروني غير صحيح.' ) );
    }
}
```

#### 3. حفظ الرسائل

##### نوع المحتوى المخصص:
- **Post Type**: `contact_submission`
- **الحالة**: private (غير قابل للوصول العام)
- **الحقول المحفوظة**:
  - `_contact_name`: اسم المرسل
  - `_contact_email`: البريد الإلكتروني
  - `_contact_phone`: رقم الهاتف
  - `_contact_subject`: موضوع الرسالة
  - `_contact_date`: تاريخ الإرسال
  - `_contact_status`: حالة الرسالة (new, read, replied, archived)
  - `_contact_ip`: عنوان IP المرسل

#### 4. الإشعارات

##### للإدارة:
- إشعار بريد إلكتروني تلقائي عند استلام رسالة جديدة
- يحتوي على:
  - معلومات المرسل
  - موضوع الرسالة
  - نص الرسالة
  - رابط مباشر لعرض الرسالة في لوحة التحكم

#### 5. عرض الرسائل في لوحة التحكم

##### الأعمدة المخصصة:
- **الاسم**: اسم المرسل
- **البريد الإلكتروني**: رابط mailto
- **الهاتف**: رابط tel
- **الموضوع**: موضوع الرسالة
- **الحالة**: 
  - جديد (new) - لون تحذير
  - مقروء (read) - لون معلومات
  - تم الرد (replied) - لون نجاح
  - مؤرشف (archived) - لون ثانوي
- **التاريخ**: تاريخ الإرسال

#### 6. معلومات التواصل

##### عرض معلومات التواصل:
- **الهاتف**: مع رابط tel
- **واتساب**: مع رابط WhatsApp
- **البريد الإلكتروني**: مع رابط mailto
- **العنوان**: بالعربية والإنجليزية
- **ساعات العمل**: بالعربية والإنجليزية

#### 7. الخرائط

##### دعم الخرائط المتعددة:
- إمكانية إضافة عدة مواقع
- لكل موقع:
  - العنوان
  - عنوان الخريطة المضمنة (iframe)
  - إذا لم يكن هناك iframe، يتم إنشاؤه تلقائياً من العنوان

#### 8. الأسئلة الشائعة (FAQ)

##### ميزات:
- قسم FAQ في صفحة التواصل
- إمكانية إضافة أسئلة وأجوبة من إعدادات القالب
- تصميم accordion تفاعلي
- أسئلة افتراضية إذا لم يتم إضافة أسئلة مخصصة

---

## صفحة حساب المستخدم

### نظرة عامة

صفحة حساب مستخدم متكاملة ومخصصة بالكامل مع جميع الوظائف اللازمة لإدارة طلبات التأشيرات والرحلات.

### الملفات المرتبطة

- **Class**: `inc/class-swa-account.php`
- **JavaScript**: `assets/js/account.js`
- **CSS**: `assets/css/account.css`
- **Auth CSS**: `assets/css/auth.css`
- **Auth JS**: `assets/js/auth.js`
- **Templates**: 
  - `templates/myaccount/visa-applications.php`
  - `templates/myaccount/visa-application-detail.php`
  - `templates/myaccount/trip-requests.php`
  - `templates/myaccount/trip-request-detail.php`

### الميزات التفصيلية

#### 1. القائمة الجانبية (Sidebar Navigation)

##### العناصر:
- **لوحة التحكم** (Dashboard)
  - نظرة عامة على الحساب
  - أحدث الطلبات
  - إحصائيات سريعة
  
- **طلبات التأشيرات** (Visa Applications)
  - عرض جميع طلبات التأشيرات
  - فلترة حسب الحالة
  - بحث في الطلبات
  
- **طلبات الرحلات** (Trip Requests)
  - عرض جميع طلبات الرحلات
  - متابعة حالة كل طلب
  
- **الطلبات والمدفوعات** (Orders)
  - طلبات WooCommerce
  - سجل المدفوعات
  
- **الملف الشخصي** (Account Details)
  - معلومات الحساب
  - تغيير كلمة المرور
  - معلومات إضافية:
    - رقم الجوال
    - الجنسية
    - رقم جواز السفر
    - اللغة المفضلة
  
- **تسجيل الخروج** (Logout)

#### 2. لوحة التحكم (Dashboard)

##### الويدجتات:
- **أحدث طلبات التأشيرات**:
  - عرض آخر 3 طلبات
  - حالة كل طلب
  - رابط مباشر للتفاصيل
  - زر لعرض جميع الطلبات

#### 3. صفحة طلبات التأشيرات

##### الميزات:
- **قائمة الطلبات**:
  - جدول يعرض جميع الطلبات
  - الأعمدة:
    - رقم الطلب
    - خدمة التأشيرة
    - الحالة
    - تاريخ الإنشاء
    - تاريخ السفر
    - الإجراءات
  
- **فلترة**:
  - حسب الحالة
  - حسب التاريخ
  
- **البحث**:
  - بحث في رقم الطلب
  - بحث في اسم الخدمة

#### 4. صفحة تفاصيل طلب التأشيرة

##### الأقسام:

###### أ. معلومات الطلب:
- رقم الطلب
- خدمة التأشيرة
- الحالة الحالية
- تاريخ الإنشاء
- تاريخ السفر
- غرض السفر

###### ب. سجل الحالات (Status Timeline):
- عرض جميع التغييرات في الحالة
- التاريخ والوقت لكل تغيير
- الملاحظات المرتبطة
- تصميم timeline تفاعلي

###### ج. معلومات المتقدمين:
- عرض جميع المتقدمين
- معلومات كل متقدم:
  - الاسم
  - رقم الجواز
  - الجنسية
  - تاريخ الميلاد
  - الجنس
- إمكانية تعديل المعلومات

###### د. المستندات:
- **المستندات المرفوعة**:
  - قائمة بجميع المستندات
  - نوع كل مستند
  - حالة المراجعة
  - إمكانية التحميل
  - إمكانية الحذف
  
- **المستندات المطلوبة**:
  - قائمة بالمستندات المطلوبة
  - إظهار المستندات المرفوعة
  - إظهار المستندات الناقصة
  
- **رفع المستندات**:
  - منطقة drag & drop
  - اختيار الملفات
  - عرض تقدم الرفع
  - رسائل نجاح/خطأ

###### هـ. التأشيرة النهائية:
- عند إصدار التأشيرة، يمكن للعميل:
  - عرض التأشيرة
  - تحميل التأشيرة
  - طباعة التأشيرة

###### و. الملاحظات:
- ملاحظات من الإدارة للعميل
- إشعارات تلقائية عند إضافة ملاحظات

#### 5. صفحة طلبات الرحلات

##### الميزات:
- عرض جميع طلبات الرحلات
- حالة كل طلب
- الوجهة
- تواريخ السفر
- رابط لعرض التفاصيل

#### 6. صفحة تفاصيل طلب الرحلة

##### الأقسام:
- معلومات العميل
- تفاصيل الرحلة
- الوجهة
- تواريخ السفر
- عدد الأفراد وأعمار الأطفال
- التفضيلات (الميزانية، مستوى الفنادق، الاهتمامات)
- الملاحظات
- الحالة الحالية

#### 7. الترجمة الكاملة

##### ترجمة WooCommerce:
- جميع نصوص WooCommerce في صفحة الحساب مترجمة
- دعم ثنائي اللغة كامل
- ترجمة تلقائية للحقول والأزرار

---

## التكامل مع WooCommerce

### نظرة عامة

تكامل كامل ومتقدم مع WooCommerce لإدارة المدفوعات والطلبات.

### الملفات المرتبطة

- **Class**: `inc/class-swa-woocommerce.php`
- **CSS**: `assets/css/woocommerce.css`
- **Template**: `woocommerce.php`
- **Login Template**: `woocommerce/myaccount/form-login.php`

### الميزات التفصيلية

#### 1. مزامنة المنتجات التلقائية

##### آلية العمل:
عند حفظ خدمة تأشيرة، يتم إنشاء/تحديث منتج WooCommerce تلقائياً:

```php
// في class-swa-woocommerce.php
public function sync_product_on_save( $post_id, $post, $update ) {
    $product_id = get_field( '_visa_woo_product', $post_id );
    $price = get_field( '_visa_price', $post_id );
    $sale_price = get_field( '_visa_sale_price', $post_id );
    
    if ( $product_id ) {
        // تحديث المنتج الموجود
        wp_update_post( array(
            'ID' => $product_id,
            'post_title' => get_field( '_visa_title_ar', $post_id ),
            // ...
        ) );
    } else {
        // إنشاء منتج جديد
        $product_id = wp_insert_post( array(
            'post_type' => 'product',
            'post_title' => get_field( '_visa_title_ar', $post_id ),
            // ...
        ) );
        
        // ربط المنتج بخدمة التأشيرة
        update_field( '_visa_woo_product', $product_id, $post_id );
    }
    
    // إعدادات المنتج
    wp_set_object_terms( $product_id, 'simple', 'product_type' );
    update_post_meta( $product_id, '_virtual', 'yes' );
    update_post_meta( $product_id, '_regular_price', $price );
    update_post_meta( $product_id, '_price', $sale_price ?: $price );
    update_post_meta( $product_id, '_visa_service_id', $post_id );
    
    // تصنيف المنتج
    $visa_cat = get_term_by( 'slug', 'visa-services', 'product_cat' );
    wp_set_object_terms( $product_id, array( $visa_cat->term_id ), 'product_cat' );
}
```

##### الميزات:
- إنشاء تلقائي للمنتج عند إضافة خدمة
- تحديث تلقائي عند تعديل الخدمة
- حذف المنتج عند حذف الخدمة
- ربط ثنائي الاتجاه
- نسخ الصورة المميزة تلقائياً

#### 2. صفحة الدفع المخصصة (Checkout)

##### الحقول الإضافية:

###### أ. معلومات طلب التأشيرة:
- **عدد المتقدمين** (مطلوب):
  - من 1 إلى 10
  - يتم استخدامه لإنشاء placeholders للمتقدمين
  
- **تاريخ السفر المتوقع** (مطلوب):
  - التحقق من أن التاريخ بعد 7 أيام على الأقل
  - يتم حفظه في الطلب
  
- **غرض السفر** (اختياري):
  - حقل نصي طويل
  - معلومات إضافية للطلب
  
- **متطلبات خاصة** (اختياري):
  - ملاحظات إضافية
  - متطلبات خاصة للعميل

##### التحقق من البيانات:
```php
public function validate_checkout_fields() {
    if ( empty( $_POST['swa_num_applicants'] ) || $_POST['swa_num_applicants'] < 1 ) {
        wc_add_notice( 'يرجى تحديد عدد المتقدمين.', 'error' );
    }
    
    if ( empty( $_POST['swa_travel_date'] ) ) {
        wc_add_notice( 'يرجى تحديد تاريخ السفر المتوقع.', 'error' );
    } else {
        $travel_date = strtotime( sanitize_text_field( $_POST['swa_travel_date'] ) );
        $min_date = strtotime( '+7 days' );
        
        if ( $travel_date < $min_date ) {
            wc_add_notice( 'تاريخ السفر يجب أن يكون بعد 7 أيام على الأقل.', 'error' );
        }
    }
}
```

#### 3. إنشاء طلب التأشيرة التلقائي

##### الأحداث التي تسبب الإنشاء:
- `woocommerce_order_status_completed`
- `woocommerce_order_status_processing`
- `woocommerce_order_status_on-hold`
- `woocommerce_payment_complete`
- `woocommerce_checkout_order_created`

##### البيانات المحفوظة:
- رقم الطلب
- معرف المستخدم
- خدمة التأشيرة
- الحالة (payment_confirmed)
- تاريخ السفر
- غرض السفر
- عدد المتقدمين
- placeholders للمتقدمين
- سجل الحالات

#### 4. صفحة الشكر المخصصة (Thank You Page)

##### الميزات:
- رسالة مخصصة عند وجود طلب تأشيرة
- رابط مباشر لصفحة طلبات التأشيرات
- توجيه العميل لاستكمال المعلومات
- تصميم جذاب مع ألوان مميزة

```php
public function thankyou_page_content( $order_id ) {
    $order = wc_get_order( $order_id );
    $application_id = $order->get_meta( '_swa_application_id' );
    
    if ( $application_id ) {
        ?>
        <div class="swa-thankyou-visa-notice">
            <h3>✓ تم إنشاء طلب التأشيرة بنجاح!</h3>
            <p>يمكنك متابعة حالة طلبك ورفع المستندات المطلوبة من لوحة التحكم.</p>
            <a href="<?php echo esc_url( wc_get_account_endpoint_url( 'visa-applications' ) ); ?>" class="button">
                عرض طلبات التأشيرات
            </a>
        </div>
        <?php
    }
}
```

#### 5. إخفاء منتجات التأشيرات من المتجر

##### الميزات:
- إخفاء منتجات التأشيرات من المتجر الرئيسي
- إخفاءها من صفحات التصنيفات
- عرضها فقط من صفحات التأشيرات المخصصة
- تصنيف خاص: `visa-services`

```php
public function hide_visa_products_from_shop( $query ) {
    if ( ! is_admin() && $query->is_main_query() && ( is_shop() || is_product_category() ) ) {
        $tax_query = (array) $query->get( 'tax_query' );
        $tax_query[] = array(
            'taxonomy' => 'product_cat',
            'field'    => 'slug',
            'terms'    => 'visa-services',
            'operator' => 'NOT IN',
        );
        $query->set( 'tax_query', $tax_query );
    }
}
```

#### 6. التوجيه المباشر للدفع

##### الميزات:
- عند إضافة منتج تأشيرة للسلة، يتم التوجيه مباشرة لصفحة الدفع
- تخطي صفحة السلة
- تجربة مستخدم محسّنة

```php
public function redirect_visa_to_checkout( $url ) {
    foreach ( WC()->cart->get_cart() as $cart_item ) {
        $product_id = $cart_item['product_id'];
        $visa_service_id = get_post_meta( $product_id, '_visa_service_id', true );
        
        if ( $visa_service_id ) {
            return wc_get_checkout_url();
        }
    }
    return $url;
}
```

#### 7. التحقق من السلة

##### الميزات:
- منع إضافة أكثر من منتج تأشيرة واحد في السلة
- رسالة توضيحية للعميل
- توجيه لاستخدام عدد المتقدمين بدلاً من ذلك

#### 8. الترجمة الكاملة

##### الميزات:
- ترجمة جميع نصوص WooCommerce
- ترجمة حقول الدفع
- ترجمة رسائل الخطأ والنجاح
- ترجمة أزرار الإجراءات

#### 9. أعمدة مخصصة في قائمة الطلبات

##### الأعمدة:
- **طلب التأشيرة**: 
  - عرض حالة طلب التأشيرة
  - رابط مباشر للطلب
  - لون حسب الحالة

#### 10. إشعارات البريد الإلكتروني

##### الميزات:
- إضافة معلومات التأشيرة لرسائل البريد الإلكتروني
- معلومات إضافية:
  - تاريخ السفر
  - عدد المتقدمين
  - رابط صفحة الحساب
- تنسيق HTML جميل

---

## الأمان في رفع الملفات

### نظرة عامة

نظام أمان متقدم ومتعدد الطبقات لحماية الملفات المرفوعة من قبل المستخدمين.

### الملفات المرتبطة

- **Class**: `inc/class-swa-documents.php`
- **JavaScript**: `assets/js/account.js` (جزء رفع الملفات)
- **Upload Directory**: `wp-content/uploads/swa-documents/`

### الميزات الأمنية التفصيلية

#### 1. مجلد محمي (Protected Directory)

##### الحماية:
- **المجلد**: `wp-content/uploads/swa-documents/`
- **حماية .htaccess**:
```apache
Order deny,allow
Deny from all
```
- **ملف index.php**: لمنع عرض محتويات المجلد
```php
<?php // Silence is golden
```

##### الميزات:
- منع الوصول المباشر للملفات
- حماية من directory listing
- حماية من الوصول غير المصرح به

#### 2. التحقق من أنواع الملفات (File Type Validation)

##### الأنواع المسموحة:
```php
private $allowed_types = array(
    'image/jpeg',      // صور JPEG
    'image/jpg',       // صور JPG
    'image/png',       // صور PNG
    'image/gif',       // صور GIF
    'application/pdf', // ملفات PDF
    'application/msword', // ملفات Word القديمة
    'application/vnd.openxmlformats-officedocument.wordprocessingml.document', // Word الحديث
);
```

##### آلية التحقق:
```php
private function validate_file( $file ) {
    // التحقق من نوع الملف
    $file_type = wp_check_filetype( $file['name'] );
    $mime_type = $file_type['type'];
    
    if ( ! in_array( $mime_type, $this->allowed_types ) ) {
        return new WP_Error( 'invalid_type', 'نوع الملف غير مسموح. يرجى رفع صور أو ملفات PDF فقط.' );
    }
    
    return true;
}
```

#### 3. التحقق من حجم الملف (File Size Validation)

##### الحد الأقصى:
- **10 ميجابايت** (10,485,760 بايت)
- قابل للتخصيص عبر filter

##### التحقق:
```php
private $max_file_size = 10485760; // 10MB

private function validate_file( $file ) {
    if ( $file['size'] > $this->max_file_size ) {
        return new WP_Error( 'file_too_large', sprintf(
            'حجم الملف يتجاوز الحد المسموح (%s ميجابايت)',
            $this->max_file_size / 1048576
        ) );
    }
    return true;
}
```

#### 4. فحص الأمان للملفات (Security Scanning)

##### الفحص:
- **فحص محتوى الملف**: البحث عن كود خبيث
- **منع الملفات الخطرة**:
  - ملفات PHP (`<?php`)
  - سكريبتات JavaScript (`<script>`)
  - دوال التنفيذ (`eval()`)

##### الكود:
```php
private function validate_file( $file ) {
    // فحص محتوى الملف
    $content = file_get_contents( $file['tmp_name'] );
    if ( preg_match( '/<\?php|<script|eval\s*\(/i', $content ) ) {
        return new WP_Error( 'malicious_file', 'الملف يحتوي على محتوى غير مسموح' );
    }
    return true;
}
```

#### 5. التحقق من الصلاحيات (Permission Check)

##### الميزات:
- التحقق من تسجيل الدخول
- التحقق من ملكية الطلب
- السماح للإدارة بالوصول لجميع الملفات

##### الكود:
```php
private function can_access_document( $doc_id ) {
    // الإدارة يمكنها الوصول لجميع الملفات
    if ( current_user_can( 'manage_options' ) || current_user_can( 'edit_posts' ) ) {
        return true;
    }
    
    $current_user_id = get_current_user_id();
    if ( ! $current_user_id ) {
        return false;
    }
    
    // التحقق من ملكية الطلب
    $app_id = get_post_meta( $doc_id, '_swa_application_id', true );
    if ( $app_id ) {
        $app_user_id = get_field( '_app_user_id', $app_id );
        return $app_user_id == $current_user_id;
    }
    
    // التحقق من رافع الملف
    $attachment = get_post( $doc_id );
    return $attachment && $attachment->post_author == $current_user_id;
}
```

#### 6. روابط آمنة للتحميل (Secure Download Links)

##### الميزات:
- روابط مؤقتة مع nonce
- التحقق من الصلاحيات قبل التحميل
- منع الوصول المباشر

##### الكود:
```php
public function get_secure_download_url( $doc_id ) {
    $nonce = wp_create_nonce( 'swa_download_' . $doc_id );
    return add_query_arg( array(
        'swa_document' => $doc_id,
        '_wpnonce'     => $nonce,
    ), home_url( '/' ) );
}

public function handle_secure_download() {
    $doc_id = get_query_var( 'swa_document' );
    $nonce = isset( $_GET['_wpnonce'] ) ? $_GET['_wpnonce'] : '';
    
    // التحقق من nonce
    if ( ! wp_verify_nonce( $nonce, 'swa_download_' . $doc_id ) ) {
        wp_die( 'رابط التحميل غير صالح أو منتهي الصلاحية.' );
    }
    
    // التحقق من الصلاحيات
    if ( ! $this->can_access_document( $doc_id ) ) {
        wp_die( 'غير مصرح لك بالوصول إلى هذا الملف.' );
    }
    
    // تقديم الملف
    $file_path = get_attached_file( $doc_id );
    header( 'Content-Type: ' . mime_content_type( $file_path ) );
    header( 'Content-Disposition: attachment; filename="' . basename( $file_path ) . '"' );
    readfile( $file_path );
    exit;
}
```

#### 7. Nonce Verification

##### الميزات:
- التحقق من nonce في جميع طلبات AJAX
- منع CSRF attacks
- حماية من الوصول غير المصرح به

##### الكود:
```php
public function ajax_upload_document() {
    // التحقق من nonce
    check_ajax_referer( 'swa_nonce', 'nonce' );
    
    // التحقق من تسجيل الدخول
    if ( ! is_user_logged_in() ) {
        wp_send_json_error( array( 'message' => 'يجب تسجيل الدخول أولاً' ) );
    }
    
    // التحقق من ملكية الطلب
    $app_id = absint( $_POST['app_id'] ?? 0 );
    if ( $app_id ) {
        $app_user_id = get_field( '_app_user_id', $app_id );
        if ( $app_user_id != get_current_user_id() && ! current_user_can( 'edit_posts' ) ) {
            wp_send_json_error( array( 'message' => 'غير مصرح' ) );
        }
    }
    
    // ... باقي الكود
}
```

#### 8. تنظيف البيانات (Data Sanitization)

##### الميزات:
- تنظيف جميع المدخلات
- استخدام دوال WordPress الآمنة
- منع XSS attacks

##### الكود:
```php
// تنظيف البيانات
$name = sanitize_text_field( $_POST['name'] );
$email = sanitize_email( $_POST['email'] );
$message = sanitize_textarea_field( $_POST['message'] );
$file_name = sanitize_file_name( $_FILES['file']['name'] );
```

#### 9. ربط الملفات بالطلبات

##### الميزات:
- ربط كل ملف بطلب محدد
- حفظ نوع المستند
- تتبع حالة المراجعة

##### الكود:
```php
// ربط الملف بالطلب
if ( $app_id ) {
    update_post_meta( $attachment_id, '_swa_application_id', $app_id );
    update_post_meta( $attachment_id, '_swa_document_type', $doc_type );
    
    // إضافة للقائمة
    $documents = get_field( '_app_documents', $app_id ) ?: array();
    $documents[] = array(
        'type'   => $doc_type,
        'file'   => $attachment_id,
        'status' => 'pending',
    );
    update_field( '_app_documents', $documents, $app_id );
}
```

#### 10. سجل الوصول (Access Logging)

##### الميزات:
- تسجيل جميع محاولات الوصول
- تسجيل رفع الملفات
- تسجيل التحميلات
- معلومات IP و timestamp

### أفضل الممارسات الأمنية المطبقة

1. ✅ **التحقق من الصلاحيات**: في كل خطوة
2. ✅ **Nonce Verification**: لجميع الإجراءات
3. ✅ **Data Sanitization**: تنظيف جميع المدخلات
4. ✅ **File Type Validation**: التحقق من النوع
5. ✅ **File Size Limits**: حدود الحجم
6. ✅ **Security Scanning**: فحص الملفات
7. ✅ **Protected Directory**: مجلد محمي
8. ✅ **Secure URLs**: روابط آمنة
9. ✅ **Access Control**: تحكم في الوصول
10. ✅ **Error Handling**: معالجة آمنة للأخطاء

---

## التكامل مع أنظمة CRM الخارجية

### نظرة عامة

الثيم مصمم بطريقة معيارية تسمح بالتكامل مع أنظمة CRM الخارجية، لكن يتطلب تطوير إضافي حسب API النظام المستخدم.

### الإمكانيات الحالية

#### 1. Hooks المتاحة للتكامل

##### Actions:
```php
// عند تغيير حالة الطلب
do_action( 'swa_application_status_changed', $app_id, $new_status, $old_status );

// عند إصدار التأشيرة
do_action( 'swa_visa_issued', $app_id, $visa_file_id );

// عند طلب مستندات
do_action( 'swa_document_required', $app_id, $required_docs );

// عند تعيين وكيل
do_action( 'swa_application_assigned', $app_id, $agent_id );

// عند إنشاء طلب جديد
do_action( 'swa_application_created', $app_id, $order_id );
```

##### Filters:
```php
// تعديل بيانات الطلب قبل الحفظ
apply_filters( 'swa_application_data', $data, $app_id );

// تعديل إعدادات SLA
apply_filters( 'swa_sla_config', $config );

// تعديل الحد الأقصى للمسافرين
apply_filters( 'swa_max_applicants', 10 );
```

#### 2. البيانات المتاحة للتصدير

##### بيانات الطلب:
```php
$application = SWA_Applications::get_application( $app_id );

// البيانات المتاحة:
- $application['id']              // معرف الطلب
- $application['order_id']         // رقم طلب WooCommerce
- $application['user_id']          // معرف المستخدم
- $application['visa_service_id']  // خدمة التأشيرة
- $application['status']           // الحالة الحالية
- $application['applicants']       // معلومات المتقدمين
- $application['travel_date']      // تاريخ السفر
- $application['documents']        // المستندات
- $application['status_history']   // سجل الحالات
```

##### بيانات الطلب (WooCommerce):
```php
$order = wc_get_order( $order_id );

// البيانات المتاحة:
- $order->get_id()
- $order->get_user_id()
- $order->get_billing_email()
- $order->get_billing_phone()
- $order->get_total()
- $order->get_date_created()
- $order->get_meta( '_swa_travel_date' )
- $order->get_meta( '_swa_num_applicants' )
```

#### 3. مثال على التكامل

##### إنشاء إضافة للتكامل مع CRM:

```php
<?php
/**
 * Plugin Name: SWA CRM Integration
 * Description: تكامل مع نظام CRM
 */

class SWA_CRM_Integration {
    
    public function __construct() {
        // عند إنشاء طلب جديد
        add_action( 'swa_application_created', array( $this, 'sync_to_crm' ), 10, 2 );
        
        // عند تغيير الحالة
        add_action( 'swa_application_status_changed', array( $this, 'update_crm_status' ), 10, 3 );
    }
    
    public function sync_to_crm( $app_id, $order_id ) {
        $application = SWA_Applications::get_application( $app_id );
        $order = wc_get_order( $order_id );
        
        // تحضير البيانات
        $crm_data = array(
            'application_id' => $app_id,
            'order_id' => $order_id,
            'customer_name' => $order->get_billing_first_name() . ' ' . $order->get_billing_last_name(),
            'customer_email' => $order->get_billing_email(),
            'customer_phone' => $order->get_billing_phone(),
            'visa_service' => get_the_title( $application['visa_service_id'] ),
            'status' => $application['status'],
            'travel_date' => $application['travel_date'],
            'applicants_count' => count( $application['applicants'] ),
            'total_amount' => $order->get_total(),
        );
        
        // إرسال لـ CRM API
        $this->send_to_crm_api( $crm_data );
    }
    
    public function update_crm_status( $app_id, $new_status, $old_status ) {
        $application = SWA_Applications::get_application( $app_id );
        
        // تحديث الحالة في CRM
        $this->update_crm_api( $app_id, array(
            'status' => $new_status,
            'status_label' => SWA_Applications::get_status_label( $new_status ),
        ) );
    }
    
    private function send_to_crm_api( $data ) {
        // هنا يتم إرسال البيانات لـ CRM
        // يتطلب معرفة API الخاص بنظام CRM المستخدم
        
        $api_url = get_option( 'swa_crm_api_url' );
        $api_key = get_option( 'swa_crm_api_key' );
        
        if ( ! $api_url || ! $api_key ) {
            return;
        }
        
        $response = wp_remote_post( $api_url . '/applications', array(
            'headers' => array(
                'Authorization' => 'Bearer ' . $api_key,
                'Content-Type' => 'application/json',
            ),
            'body' => json_encode( $data ),
            'timeout' => 30,
        ) );
        
        if ( is_wp_error( $response ) ) {
            error_log( 'SWA CRM Integration Error: ' . $response->get_error_message() );
        }
    }
    
    private function update_crm_api( $app_id, $data ) {
        // تحديث البيانات في CRM
        // يتطلب معرفة API الخاص بنظام CRM المستخدم
    }
}

new SWA_CRM_Integration();
```

### المتطلبات للتكامل

#### 1. معرفة API النظام
- **رابط API**: يجب معرفة رابط API الخاص بنظام CRM
- **طريقة المصادقة**: Bearer Token, API Key, OAuth, etc.
- **نقاط النهاية (Endpoints)**:
  - إنشاء سجل جديد
  - تحديث سجل موجود
  - جلب البيانات
  - حذف سجل

#### 2. هيكل البيانات
- **تنسيق البيانات**: JSON, XML, etc.
- **الحقول المطلوبة**: ما هي الحقول التي يتوقعها CRM
- **التنسيق**: تنسيق التواريخ، الأرقام، etc.

#### 3. التطوير المطلوب
- **إنشاء إضافة مخصصة**: للتكامل مع CRM المحدد
- **معالجة الأخطاء**: التعامل مع أخطاء API
- **إعادة المحاولة**: في حالة فشل الإرسال
- **سجل العمليات**: تسجيل جميع محاولات التكامل

### الخطوات الموصى بها

1. **تحليل API النظام**:
   - مراجعة وثائق API
   - فهم هيكل البيانات
   - اختبار API يدوياً

2. **تطوير الإضافة**:
   - استخدام الـ hooks المتاحة
   - إعدادات في لوحة التحكم
   - معالجة الأخطاء

3. **الاختبار**:
   - اختبار في بيئة التطوير
   - اختبار جميع السيناريوهات
   - اختبار معالجة الأخطاء

4. **النشر**:
   - نشر في بيئة الإنتاج
   - مراقبة السجلات
   - ضبط الإعدادات

### ملاحظات مهمة

⚠️ **يتطلب تطوير إضافي**: التكامل مع CRM يحتاج إلى تطوير مخصص حسب API النظام المستخدم.

⚠️ **لا يوجد تكامل جاهز**: الثيم لا يحتوي على تكامل جاهز مع أي نظام CRM محدد.

✅ **جاهز للتكامل**: البنية التحتية جاهزة ويمكن التكامل بسهولة.

---

## جميع الميزات بالتفصيل

### 1. نظام الفلاتر والبحث المتقدم

#### الملفات:
- **Class**: `inc/class-swa-ajax-filter.php`
- **JavaScript**: `assets/js/filter.js`
- **CSS**: `assets/css/filter.css`

#### الميزات:
- **البحث الفوري**: نتائج ديناميكية أثناء الكتابة
- **فلترة متعددة**:
  - حسب الدولة
  - حسب نوع التأشيرة (متعدد)
  - حسب نطاق السعر
  - حسب مدة المعالجة
  - حسب الأهلية
- **الترتيب**:
  - الأحدث
  - السعر: من الأقل للأعلى
  - السعر: من الأعلى للأقل
  - الأسرع معالجة
  - الأكثر طلباً
- **العد الديناميكي**: عرض عدد النتائج لكل فلتر
- **Shortcodes**:
  - `[swa_visa_filter]`: عرض فلتر كامل
  - `[swa_visa_search]`: عرض شريط بحث

### 2. نظام طلبات العائلات

#### الملفات:
- **Class**: `inc/class-swa-family-applications.php`
- **JavaScript**: `assets/js/family-applications.js`
- **CSS**: `assets/css/family-applications.css`

#### الميزات:
- **إضافة مسافرين متعددين**: حتى 10 مسافرين
- **نموذج ديناميكي**: إضافة/حذف مسافرين
- **الخصم العائلي**:
  - 5% لمسافرين
  - 10% لـ 3-4 مسافرين
  - 15% لـ 5+ مسافرين
- **حفظ في السلة**: حفظ بيانات المسافرين في سلة WooCommerce
- **عرض في الدفع**: عرض معلومات المسافرين في صفحة الدفع

### 3. نظام SLA (Service Level Agreement)

#### الملفات:
- **Class**: `inc/class-swa-sla.php`
- **CSS**: `assets/css/sla.css`
- **Template**: `templates/admin/sla.php`

#### الميزات:
- **إعدادات SLA قابلة للتخصيص**:
  - وقت الاستجابة الأول: 4 ساعات
  - مراجعة المستندات: 24 ساعة
  - بدء المعالجة: 48 ساعة
  - الإنجاز حسب نوع التأشيرة
- **مستويات التصعيد**:
  - تحذير (80% من الوقت)
  - حرج (100% من الوقت)
  - تجاوز (120% من الوقت)
- **فحص تلقائي**: كل ساعة
- **إشعارات تلقائية**: عند التجاوز
- **لوحة SLA**: إحصائيات شاملة

### 4. إدارة الوكلاء

#### الميزات:
- **تعيين الوكلاء**: تلقائي أو يدوي
- **توزيع عادل**: توزيع الطلبات على الوكلاء
- **حد أقصى**: تحديد حد أقصى للطلبات لكل وكيل
- **إحصائيات الأداء**:
  - عدد الطلبات النشطة
  - عدد الطلبات المكتملة
  - معدل الالتزام بـ SLA
  - متوسط وقت الإنجاز
- **التصعيد**: تصعيد الطلبات المتجاوزة

### 5. التكامل مع WhatsApp

#### الملفات:
- **Class**: `inc/class-swa-whatsapp.php`

#### الميزات:
- **إرسال تلقائي**:
  - عند تأكيد الدفع
  - عند تغيير الحالة
  - عند إصدار التأشيرة
  - عند طلب مستندات
- **إعدادات API**:
  - رابط API
  - رمز API
  - تفعيل/تعطيل
- **اختبار الاتصال**: اختبار API
- **سجل الرسائل**: تتبع جميع الرسائل المرسلة

### 6. نظام الاحتفاظ بالبيانات

#### الملفات:
- **Class**: `inc/class-swa-retention.php`

#### الميزات:
- **تنظيف تلقائي**: حذف البيانات القديمة
- **إعدادات قابلة للتخصيص**:
  - فترة الاحتفاظ (افتراضي: 365 يوم)
  - حذف المستندات
  - حذف الطلبات
- **إشعارات قبل الحذف**: إشعار قبل 30 يوم
- **معاينة الحذف**: عرض الملفات المراد حذفها
- **سجل الحذف**: تسجيل جميع عمليات الحذف

### 7. التكامل مع Elementor

#### الملفات:
- **Class**: `inc/class-swa-elementor.php`
- **Widgets**: `inc/elementor/widgets/`

#### الويدجتات المتاحة:
1. **Visa Card Widget**: بطاقة تأشيرة واحدة
2. **Visa Grid Widget**: شبكة من التأشيرات
3. **Visa Filter Widget**: فلتر متقدم
4. **Visa Carousel Widget**: كاروسيل التأشيرات
5. **Country List Widget**: قائمة الدول
6. **Requirements Widget**: متطلبات التأشيرة
7. **Destination Carousel Widget**: كاروسيل الوجهات

### 8. نظام SEO المتقدم

#### الميزات:
- **Schema Markup كامل**:
  - Organization Schema
  - Product Schema
  - Article Schema
  - Breadcrumb Schema
  - Place Schema
- **Meta Tags**:
  - Open Graph
  - Twitter Cards
  - Canonical URLs
- **التوافق**: مع Yoast SEO

### 9. نظام الإشعارات

#### أنواع الإشعارات:
- **بريد إلكتروني**:
  - إشعارات تلقائية عند تغيير الحالة
  - إشعارات طلب المستندات
  - إشعارات إصدار التأشيرة
- **WhatsApp** (اختياري):
  - نفس الإشعارات عبر WhatsApp
- **في لوحة التحكم**:
  - إشعارات للعملاء
  - إشعارات للإدارة

### 10. نظام التقارير

#### الميزات:
- **إحصائيات شاملة**:
  - إجمالي الطلبات
  - الطلبات حسب الحالة
  - الطلبات حسب الفترة الزمنية
  - أداء الوكلاء
  - معدل الالتزام بـ SLA
- **تصدير التقارير**:
  - CSV
  - Excel (قابل للتطوير)

---

## التحسينات من النظام القديم

### النظام القديم: Trip Engine Plugin

#### القيود:
- ❌ مصمم للرحلات وليس للتأشيرات
- ❌ محدود في التخصيص
- ❌ تكامل محدود مع WooCommerce
- ❌ لا يدعم طلبات التأشيرات المتعددة
- ❌ نظام مستندات بسيط
- ❌ لا يوجد نظام SLA
- ❌ لا يوجد إدارة وكلاء
- ❌ تكامل محدود مع WhatsApp

### النظام الجديد: SWA Visa Commerce Theme

#### التحسينات:

##### 1. حل مخصص بالكامل
- ✅ مصمم خصيصاً لخدمات التأشيرات
- ✅ مرونة كاملة في التخصيص
- ✅ تكامل كامل مع جميع الأنظمة

##### 2. نظام طلبات متقدم
- ✅ نظام حالات متقدم (11 حالة)
- ✅ سجل كامل لتاريخ التغييرات
- ✅ تتبع دقيق للطلبات
- ✅ إشعارات تلقائية

##### 3. إدارة مستندات آمنة
- ✅ نظام أمان متعدد الطبقات
- ✅ مجلد محمي
- ✅ فحص الملفات
- ✅ روابط آمنة

##### 4. تكامل WooCommerce كامل
- ✅ مزامنة تلقائية للمنتجات
- ✅ حقول مخصصة في الدفع
- ✅ إنشاء تلقائي للطلبات
- ✅ ترجمة كاملة

##### 5. صفحة حساب مستخدم متكاملة
- ✅ عرض جميع الطلبات
- ✅ رفع المستندات
- ✅ متابعة الحالات
- ✅ تفاصيل كاملة

##### 6. صفحة طلب رحلة مخصصة
- ✅ نموذج شامل ومفصل
- ✅ دعم الوجهات المخصصة
- ✅ معلومات الأفراد الكاملة
- ✅ التفضيلات والاهتمامات

##### 7. نظام SLA وإدارة الوكلاء
- ✅ تتبع الالتزام بـ SLA
- ✅ تصعيد تلقائي
- ✅ إدارة الوكلاء
- ✅ إحصائيات الأداء

##### 8. نظام العائلات
- ✅ دعم متقدم للمسافرين المتعددين
- ✅ خصومات عائلية
- ✅ إدارة سهلة

##### 9. نظام البحث والفلترة
- ✅ بحث فوري
- ✅ فلترة متعددة
- ✅ نتائج ديناميكية

##### 10. التكامل مع Elementor
- ✅ ويدجتات مخصصة
- ✅ تصميم سهل
- ✅ مرونة كاملة

### الخلاصة

تم تطوير ثيم SWA Visa Commerce كحل شامل ومتقدم بدلاً من الاعتماد على إضافة Trip Engine Plugin. يوفر النظام جميع الميزات المطلوبة وأكثر، مع مرونة كاملة في التخصيص والتطوير المستقبلي.

---

## الخلاصة النهائية

ثيم **SWA Visa Commerce** هو حل متكامل ومتطور بالكامل لإدارة خدمات التأشيرات. يوفر:

✅ **نظام إدارة طلبات متقدم** مع تتبع كامل للحالات  
✅ **تكامل كامل مع WooCommerce** للمدفوعات والطلبات  
✅ **نظام أمان متعدد الطبقات** لرفع الملفات  
✅ **صفحة حساب مستخدم متكاملة** مع جميع الوظائف  
✅ **صفحة طلب رحلة مخصصة** بدلاً من Trip Engine Plugin  
✅ **نموذج تواصل متكامل** مع إدارة كاملة  
✅ **نظام SLA وإدارة الوكلاء** متقدم  
✅ **دعم العائلات والمسافرين المتعددين**  
✅ **تكامل مع WhatsApp** للإشعارات  
✅ **نظام بحث وفلترة متقدم**  
✅ **تكامل مع Elementor** مع ويدجتات مخصصة  
✅ **جاهز للتكامل مع CRM** (يتطلب تطوير إضافي)  
✅ **دعم ثنائي اللغة كامل** (عربي/إنجليزي)  
✅ **تصميم متجاوب** لجميع الأجهزة  
✅ **SEO محسّن** مع Schema Markup كامل  

---

**الإصدار**: 1.0.0  
**تاريخ التحديث**: 19/1/2026  
**المطور**: Bakry Abdelsalam
**الويبسايت**: https://bakry.site
**الترخيص**: GNU General Public License v2 or later

---

## ملاحظات مهمة

### التكامل مع CRM
⚠️ **يتطلب تطوير إضافي**: التكامل مع أنظمة CRM الخارجية يحتاج إلى تطوير مخصص حسب API النظام المستخدم. الثيم يوفر الـ hooks والبنية التحتية اللازمة، لكن يحتاج معرفة API النظام لتنفيذ التكامل.

### الإضافات المطلوبة
- **WooCommerce**: للتجارة الإلكترونية
- **Advanced Custom Fields (ACF)**: للحقول المخصصة
- **Elementor** (اختياري): لبناء الصفحات

### الإضافات الموصى بها
- **WPML** أو **Polylang**: لدعم متعدد اللغات
- **Yoast SEO**: لتحسين محركات البحث
- **WP Rocket**: لتحسين الأداء

---

**نهاية التوثيق**
