# دليل تكامل Bitrix24 CRM مع موقع SWA

## نظرة عامة

يوفر هذا التكامل ربطاً مباشراً وتلقائياً بين موقع SWA ونظام Bitrix24 CRM. جميع الطلبات والاستفسارات التي تصل عبر الموقع يتم إرسالها فوراً إلى Bitrix24 ليتمكن الموظفون من إدارتها ومتابعتها من لوحة تحكم CRM واحدة.

---

## ما الذي يتم إرساله إلى Bitrix24؟

| مصدر الطلب | ما يُنشأ في Bitrix24 | التفاصيل المرسلة |
|---|---|---|
| **نموذج اتصل بنا** | Lead (عميل محتمل) | الاسم، البريد، الهاتف، الموضوع، الرسالة |
| **طلب رحلة** | Lead + Deal (صفقة) + Contact (جهة اتصال) | بيانات المسافر، الوجهة، التواريخ، الميزانية، عدد المسافرين |
| **طلب تأشيرة (WooCommerce)** | Deal (صفقة) + Contact (جهة اتصال) | نوع التأشيرة، الدولة، المستندات المطلوبة، مدة المعالجة، تاريخ السفر، الغرض، عدد المتقدمين، المبلغ، العملة |
| **تغيير حالة الطلب** | تحديث مرحلة الصفقة (Deal Stage) | الحالة القديمة → الحالة الجديدة |

---

## المتطلبات

- حساب Bitrix24 (مجاني أو مدفوع) — [bitrix24.com](https://www.bitrix24.com)
- صلاحية مدير (Administrator) في Bitrix24 لإنشاء Webhook
- صلاحية مدير (manage_options) في ووردبريس للوصول لصفحة الإعدادات

---

## خطوات الإعداد

### الخطوة 1: إنشاء Webhook في Bitrix24

1. سجّل الدخول إلى حساب **Bitrix24** الخاص بشركتك
2. من القائمة الجانبية، اذهب إلى:
   ```
   الإعدادات → التكامل → REST API
   ```
   أو بالإنجليزية:
   ```
   Settings → Integrations → REST API
   ```
3. اضغط على **"إضافة Inbound Webhook"** (أو "Add Inbound Webhook")
4. أدخل المعلومات التالية:
   - **الاسم**: `SWA Website Integration`
   - **الوصف**: `تكامل موقع SWA مع Bitrix24`
5. في قسم **الصلاحيات (Permissions)**، فعّل:
   - `CRM` — إدارة العملاء المحتملين والصفقات وجهات الاتصال
6. اضغط **حفظ**
7. ستحصل على رابط مثل:
   ```
   https://yourcompany.bitrix24.com/rest/1/abc123xyz456/
   ```
   **انسخ هذا الرابط** — ستحتاجه في الخطوة التالية

### الخطوة 2: إعداد التكامل في ووردبريس

1. سجّل الدخول إلى **لوحة تحكم ووردبريس**
2. من القائمة الجانبية، اذهب إلى:
   ```
   SWA Dashboard → Bitrix24 CRM
   ```
3. الصق **رابط الويب هوك** الذي نسخته في حقل "رابط الويب هوك (Webhook URL)"
4. اضغط زر **"اختبار الاتصال"**
   - إذا ظهرت رسالة **"تم الاتصال بنجاح"** ✓ — الرابط صحيح
   - إذا ظهر خطأ — تأكد من صحة الرابط والصلاحيات
5. فعّل مفتاح **"تفعيل التكامل"**
6. اختر ما تريد إرساله إلى Bitrix24:
   - ✅ نموذج الاتصال → Lead
   - ✅ طلب رحلة → Lead + Deal
   - ✅ طلب تأشيرة (WooCommerce) → Deal
   - ✅ مزامنة حالة الطلبات
7. حدد **معرف الموظف المسؤول** (ID) — هذا الموظف سيتم تعيين جميع الطلبات الجديدة له
   - الافتراضي: `1` (المدير الرئيسي)
   - لمعرفة ID الموظف: في Bitrix24 اذهب لملفه الشخصي — الرقم في الرابط هو الـ ID
8. اضغط **"حفظ الإعدادات"**

---

## كيف يعمل التكامل؟

### سيناريو 1: زائر يرسل نموذج اتصال

```
الزائر يملأ نموذج "اتصل بنا" → يضغط إرسال
    ↓
ووردبريس يحفظ الرسالة + يرسل إيميل للمدير
    ↓
تلقائياً: يُنشأ Lead جديد في Bitrix24
    ↓
الموظف يرى Lead جديد في لوحة CRM → يتواصل مع العميل
```

### سيناريو 2: عميل يطلب رحلة

```
العميل يملأ نموذج "طلب رحلة" → يضغط إرسال
    ↓
ووردبريس يحفظ الطلب + يرسل إيميل للمدير
    ↓
تلقائياً في Bitrix24:
  1. يُنشأ Lead (عميل محتمل)
  2. يُنشأ أو يُربط Contact (جهة اتصال)
  3. يُنشأ Deal (صفقة) بمرحلة "جديد"
    ↓
الموظف يرى الصفقة الجديدة → يحضّر عرض سعر → يتابع العميل
```

### سيناريو 3: عميل يشتري تأشيرة

```
العميل يشتري خدمة تأشيرة من الموقع → يدفع عبر WooCommerce
    ↓
ووردبريس ينشئ الطلب + طلب التأشيرة
    ↓
تلقائياً في Bitrix24:
  1. يُنشأ أو يُربط Contact (جهة اتصال بالاسم والهاتف والبريد)
  2. يُنشأ Deal (صفقة) تحتوي على:
     - عنوان يشمل اسم خدمة التأشيرة واسم العميل
     - المبلغ والعملة
     - نوع التأشيرة والدولة
     - مدة المعالجة
     - قائمة المستندات المطلوبة
     - تاريخ السفر والغرض
     - عدد المتقدمين
     - حالة الطلب
    ↓
عند تغيير حالة الطلب (قيد المعالجة، مكتمل، ملغي...):
  → يتم تحديث مرحلة الصفقة في Bitrix24 تلقائياً
```

---

## خريطة الحالات (Status Mapping)

### حالات طلبات WooCommerce → مراحل Bitrix24

| حالة WooCommerce | مرحلة Bitrix24 (Deal Stage) |
|---|---|
| معلق (pending) | NEW — جديد |
| قيد الانتظار (on-hold) | PREPARATION — تحضير |
| قيد التنفيذ (processing) | EXECUTING — تنفيذ |
| مكتمل (completed) | WON — تم الفوز |
| ملغي (cancelled) | LOSE — خسارة |
| مسترد (refunded) | LOSE — خسارة |
| فاشل (failed) | LOSE — خسارة |

### حالات طلبات التأشيرة → مراحل Bitrix24

| حالة طلب التأشيرة | مرحلة Bitrix24 |
|---|---|
| تم الإرسال (submitted) | NEW |
| تم تأكيد الدفع (payment_confirmed) | PREPAYMENT_INVOICE |
| مراجعة المستندات (documents_review) | PREPARATION |
| مستندات ناقصة (missing_documents) | PREPARATION |
| تم الإرسال للسفارة (submitted_embassy) | EXECUTING |
| قيد المعالجة (under_processing) | EXECUTING |
| تمت الموافقة (approved) | FINAL_INVOICE |
| تم إصدار التأشيرة (visa_issued) | WON |
| مكتمل (completed) | WON |
| مرفوض (rejected) | LOSE |
| ملغي (cancelled) | LOSE |

---

## إعدادات Bitrix24 المُوصى بها

### إعداد Pipeline (مسار المبيعات) مخصص

لتجربة أفضل، يمكنك إنشاء Pipeline مخصص في Bitrix24:

1. اذهب إلى **CRM → الإعدادات → مسار المبيعات**
2. أنشئ مساراً جديداً باسم **"طلبات الموقع"**
3. أضف المراحل التالية:

| المرحلة | اللون المقترح |
|---|---|
| طلب جديد | 🔵 أزرق |
| تم التواصل | 🟡 أصفر |
| عرض سعر مُقدم | 🟠 برتقالي |
| بانتظار المستندات | 🟡 أصفر |
| تم الإرسال للسفارة | 🔵 أزرق |
| قيد المعالجة | 🟣 بنفسجي |
| تم الإصدار | 🟢 أخضر |
| مكتمل | ✅ أخضر غامق |

### إعداد الإشعارات في Bitrix24

لضمان متابعة فورية من الموظفين:

1. **إشعارات فورية**: اذهب إلى إعدادات CRM → تفعيل إشعارات الصفقات الجديدة
2. **قواعد أتمتة**: أنشئ Automation Rule لإرسال إشعار عند إنشاء Lead أو Deal جديد
3. **تعيين تلقائي**: يمكنك إعداد توزيع تلقائي للطلبات بين الموظفين

---

## سجل العمليات والمراقبة

### عرض سجل العمليات

في صفحة **SWA Dashboard → Bitrix24 CRM** يظهر:

- **الحالة**: متصل / غير متصل
- **إجمالي المرسل**: عدد العمليات الناجحة
- **آخر إرسال**: تاريخ ووقت آخر عملية
- **سجل العمليات**: آخر 50 عملية مع النتيجة و Bitrix24 ID

### في حال وجود أخطاء

| الخطأ | السبب | الحل |
|---|---|---|
| `INVALID_TOKEN` | رابط الويب هوك غير صحيح أو منتهي | أنشئ Webhook جديد وحدّث الرابط |
| `INSUFFICIENT_SCOPE` | صلاحيات ناقصة | أضف صلاحية `CRM` للويب هوك |
| `Connection timeout` | بطء الاتصال بـ Bitrix24 | تحقق من اتصال الخادم بالإنترنت |
| `Invalid JSON response` | استجابة غير متوقعة | تحقق من صحة رابط الويب هوك |

---

## الملفات التقنية

### الملفات المُنشأة

| الملف | الوظيفة |
|---|---|
| `inc/class-swa-bitrix24.php` | الكلاس الرئيسي للتكامل مع Bitrix24 REST API |
| `assets/css/bitrix24-admin.css` | تنسيقات صفحة الإعدادات |
| `assets/js/bitrix24-admin.js` | JavaScript لاختبار الاتصال في صفحة الإعدادات |

### الملفات المُعدَّلة

| الملف | التعديل |
|---|---|
| `functions.php` | إضافة تحميل وتهيئة كلاس Bitrix24 |
| `inc/class-swa-contact.php` | إضافة `do_action('swa_contact_form_submitted')` بعد حفظ الرسالة |
| `inc/class-swa-trip-requests.php` | إضافة `do_action('swa_trip_request_submitted')` بعد حفظ الطلب |

### Hooks المتاحة للمطورين

```php
// يُنفَّذ بعد إرسال نموذج اتصال
do_action( 'swa_contact_form_submitted', $submission_id, $data );

// يُنفَّذ بعد إرسال طلب رحلة
do_action( 'swa_trip_request_submitted', $request_id, $data );

// يُنفَّذ عند تغيير حالة طلب التأشيرة (موجود مسبقاً)
do_action( 'swa_application_status_changed', $app_id, $new_status, $old_status );
```

### إعدادات ووردبريس (wp_options)

| الخيار | النوع | الوصف |
|---|---|---|
| `swa_bitrix24_enabled` | boolean | تفعيل/تعطيل التكامل |
| `swa_bitrix24_webhook_url` | string | رابط الويب هوك |
| `swa_bitrix24_assigned_by` | integer | معرف الموظف المسؤول |
| `swa_bitrix24_lead_contact` | boolean | إرسال نماذج الاتصال |
| `swa_bitrix24_lead_trip` | boolean | إرسال طلبات الرحلات |
| `swa_bitrix24_deal_order` | boolean | إرسال طلبات WooCommerce |
| `swa_bitrix24_sync_status` | boolean | مزامنة تغييرات الحالة |
| `swa_bitrix24_log` | array | سجل العمليات (آخر 200) |
| `swa_bitrix24_total_sent` | integer | عدّاد العمليات الناجحة |
| `swa_bitrix24_last_sent` | string | تاريخ آخر إرسال ناجح |
| `swa_bitrix24_last_error` | string | آخر رسالة خطأ |

### Meta Fields المُضافة

| الحقل | يُخزَّن في | الوصف |
|---|---|---|
| `_bitrix24_lead_id` | رسالة التواصل / طلب الرحلة | معرف الـ Lead في Bitrix24 |
| `_bitrix24_deal_id` | طلب الرحلة / طلب WooCommerce | معرف الـ Deal في Bitrix24 |

---

## استكشاف الأخطاء

### التكامل لا يعمل

1. تأكد أن **"تفعيل التكامل"** مُفعَّل ✅
2. تأكد من **صحة رابط الويب هوك** — يجب أن ينتهي بـ `/`
3. اضغط **"اختبار الاتصال"** — يجب أن تظهر رسالة نجاح
4. تأكد أن الخادم يستطيع الاتصال بـ `bitrix24.com` (لا يوجد جدار ناري يمنع الاتصال)

### الطلبات لا تظهر في Bitrix24

1. افحص **سجل العمليات** في صفحة الإعدادات
2. تأكد من تفعيل النوع المناسب (Lead / Deal / مزامنة)
3. تحقق من أن الويب هوك لديه صلاحية `CRM`

### بطء في إرسال النماذج

التكامل يستخدم اتصالاً متزامناً بحد أقصى 10 ثوانٍ. إذا كان Bitrix24 بطيئاً، قد يشعر المستخدم بتأخير بسيط عند إرسال النموذج. البيانات تُحفظ في ووردبريس أولاً، ثم تُرسل إلى Bitrix24.

### الويب هوك توقف عن العمل

أحياناً تنتهي صلاحية الويب هوك. الحل:
1. اذهب إلى Bitrix24 → التكامل → REST API
2. تحقق من حالة الويب هوك
3. إذا كان معطلاً، أنشئ واحداً جديداً
4. حدّث الرابط في إعدادات ووردبريس

---

## الأمان

- رابط الويب هوك يُخزَّن في قاعدة بيانات ووردبريس (جدول `wp_options`)
- صفحة الإعدادات متاحة فقط للمديرين (صلاحية `manage_options`)
- اختبار الاتصال يتطلب nonce صالح
- لا يتم إرسال كلمات مرور أو بيانات حساسة إلى Bitrix24
- جميع البيانات المُرسلة تمر عبر HTTPS (إذا كان Bitrix24 يستخدم SSL)

---

## الدعم

للمساعدة في إعداد التكامل أو حل مشكلة:

1. افحص **سجل العمليات** في صفحة الإعدادات
2. راجع قسم **استكشاف الأخطاء** أعلاه
3. تواصل مع فريق التطوير مع تقديم:
   - لقطة شاشة من سجل العمليات
   - رسالة الخطأ الظاهرة
   - نوع الطلب الذي لم يُرسل (اتصال / رحلة / تأشيرة)
